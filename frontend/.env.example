# Public API gateway URL (ONLY ENTRY POINT)
# Local development:
VITE_GATEWAY_TARGET=http://localhost:4000

# Production (set on Netlify → Site settings → Environment variables):
# VITE_GATEWAY_TARGET=https://digital-library-gateway-production.up.railway.app

# Optional legacy (not used anymore)
# VITE_API_BASE_URL=http://localhost:4000