{
  "name": "digital-library-postman-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Postman + Newman integration tests for the Digital Library API Gateway.",
  "scripts": {
    "test": "newman run digital-library.postman_collection.json -e local.postman_environment.json --color on",
    "test:ci": "newman run digital-library.postman_collection.json -e local.postman_environment.json --reporters cli,junit --reporter-junit-export ./reports/newman.junit.xml --color off"
  },
  "devDependencies": {
    "newman": "^6.2.1"
  }
}
